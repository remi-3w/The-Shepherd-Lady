{% extends 'base.html.twig' %}

{% block title %}Mes commandes{% endblock %}
{% block body %}


<div class="container">


<h1>Mes commandes</h1>

<table class="table">
    <thead>
        <tr>
            <th>Numéros</th>
            <th>Adresse</th>
            <th>Date de commande</th>
            <th>Total</th>
        </tr>
        <tr>
        {% for p in purchases %}
            <td>{{ p.id }}</td>
            <td>{{ p.adresse }}</td>
            <td>{{ p.purcharseAt |date('d/m/Y H:i') }}</td>
            <td>{{ p.total }}</td>
        {% endfor %}        
        </tr>
    </thead>
</table>
</div>

 


{% endblock %}
