{% extends 'base.html.twig' %}

{% block title %}Mes commandes{% endblock %}
{% block body %}


<div class="container">


<h1>Mes commandes</h1>

<table class="table">
    <thead>
        <tr>
            <th>Numéros</th>
            <th>Adresse</th>
            <th>Date de commande</th>
            <th>Total</th>
        </tr>
        </thead>
        </tbody>
            {% for p in purchases %}
                <tr>        
                    <td>{{ p.id }}</td>
                    <td>{{ p.adress }}</td>       
                    <td>{{ p.PurchaseAt|date('d/m/Y H:i') }}</td>
                    <td>{{ p.total }}</td>
                </tr>
           {% endfor %}
        </tbody>
</table>
</div>

 


{% endblock %}
